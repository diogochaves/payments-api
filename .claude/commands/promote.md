Read `prodops/skills/promote/SKILL.md`.

If "$ARGUMENTS" is not empty, check the Steps table in that file.
If "$ARGUMENTS" matches a listed step, read and execute only the corresponding step file — do not run the full flow.
If "$ARGUMENTS" does not match any listed step, run the full flow.
If "$ARGUMENTS" is empty, run the full flow.
