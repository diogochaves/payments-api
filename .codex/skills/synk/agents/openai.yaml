interface:
  display_name: "SYNK"
  short_description: "Synchronize branches safely"
  default_prompt: "Use $synk to fetch the latest remote code, update the local base, bring it into the current branch, resolve conflicts, and validate the result."
policy:
  allow_implicit_invocation: true
