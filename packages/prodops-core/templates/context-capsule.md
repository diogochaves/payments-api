# Card: <slug>

> Generated by `/bootstrap` — do not edit by hand. A stale capsule is worse
> than none: regenerate instead of patching.
> Location: `paths.cards` from `manifest.yaml` → `<slug>/context.md`

## Contract

- **OBC:** `<path>` — success criteria, verbatim:
  - <the 3–6 measurable lines from the OBC>
- **BDD:** `<path>` — scenarios:
  1. <one line per scenario>
  2. <...>

## Risk & reliability

- **Risks (this card only):** <one line each, with ID>
- **Reliability:** <SLOs, timeouts, idempotency requirements for this card>

## Implementation surface

- **Contract surface:** <endpoint(s) or interface(s), request/response essentials>
- **Gates:** run by name from `manifest.yaml` — `lint`, `unit`, `acceptance`
  (when behavior/contract changed), `no_mocks`, `smoke`.
- **Branch:** `<type>/<slug>`  **Base:** `<base-branch>`

## Open questions

<Anything marked pending. The agent must not resolve these silently — missing
context means stop and report.>

- [ ]
